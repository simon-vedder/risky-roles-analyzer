# Verification log

What ran, on what, and how long it took. Every row is a real run; nothing here is extrapolated.

| Date | Scenario | Target | Result |
|---|---|---|---|
| 2026-09-06 | Full Pester suite on the synthetic tenant fixture (15 findings across two subscriptions, inherited MG assignment, nested group cycle, custom roles, dormant and deactivated apps, disabled user, PIM eligibility) | Local, PowerShell 7 on macOS, Pester 6.0.1, Az.Resources 10.0.0, Microsoft.Graph.Authentication 2.36.1 | 109 passed, 0 failed, 5 s |
| 2026-09-06 | Suite extended: report renderer, `Export-`, `Show-`, `Restore-`, PIM activation (16 findings) | Local, as above, Pester 6.0.1 and 5.7.1 | 130 passed, 0 failed, 3 s |
| 2026-09-06 | `Export-RiskyRoleReport` on 14 synthetic findings, opened in headless Chrome | macOS, Chrome headless 1600 px | All rows rendered, cards and filters populated, screenshot in `docs/images/report.png` |
| 2026-09-06 | Read path on a real tenant: `Connect-RiskyRolesAnalyzer` (browser sign-in, four read scopes), `Get-RiskyRoleAssignment -Verbose`, `Export-RiskyRoleReport` | Own tenant (Entra ID P2, Azure subscriptions with root, subscription and resource scopes), macOS, module from the working tree | 8 findings in 56 s: 4 Critical, 3 High, 1 Low; Azure root and subscription assignments, Entra permanent and PIM eligible seen; 0 unresolved principals; auditing identity protected; the break-glass account was unprotected because `-BreakGlassAccount` was not passed, which led to the name warning |
| 2026-09-06 | Write path on a real tenant with a test user who holds nothing else: Reader on a throwaway resource group (Azure) and Directory Readers tenant-wide (Entra), declared privileged with `-AdditionalAzureRole` / `-AdditionalEntraRole`; `Remove -WhatIf`, `Remove -Confirm:$false`, both assignments verified gone through Az and Graph, `Restore -Path` from the backup file, both verified back, then everything deleted | Own tenant, `Connect-RiskyRolesAnalyzer -RequestWriteScopes`, macOS, module from the working tree | 11 findings in 26 s with `-SubscriptionId`; 2 for the test user; `-WhatIf` wrote no backup; 2 removed, 2 restored; the restored Entra assignment came back with the same id (ids are derived from principal, role and scope); cleanup left nothing behind |
| 2026-09-07 | Custom role rating on Az.Resources 10: the first run of the scenario below failed to rate the custom role at all, because Az.Resources 10.0.0 returns role definitions with `Permissions[]` instead of flattened `Actions`; collector fixed (`Get-RoleDefinitionPermission`), three fixture tests added | Local, Az.Resources 10.0.0 and 8.0.0 installed | 135 Pester tests passed, PSScriptAnalyzer clean |
| 2026-09-07 | Group expansion, custom role, disabled user, refuse/remove/restore on a real tenant: throwaway resource group, custom role `RRA Test Role Assigner` (`roleAssignments/write`, assignable only in that group), security group with the test user as member, role assigned to the group and directly to the user, user disabled, `Get-RiskyRoleAssignment -SubscriptionId -SkipEntra`, `Remove-` on all three findings, `Restore-` from the backup, cleanup, user re-enabled | Own tenant, Graph session built from the Az access token (reads role assignments, not PIM schedules), macOS, module from the working tree | 6 findings in 6 s, 3 on the resource group: group 5.9 Medium, member via group 3.9 Low protected ("Inherited through group"), direct 3.9 Low removable, both user findings `Disabled`; `Remove-` skipped the inherited one and removed two; backup held two entries; `Restore-` put both back (custom role by definition id, group principal); 15 checks passed, nothing left behind |
| 2026-09-07 | Report UI in headless Chrome over the DevTools protocol on the 14-finding sample: every filter, search, empty state, three sorts, select visible / clear / single row, removal popup content and ids, clipboard write and read-back, cleanup popup for inherited and for removable Entra rows, click-outside close, accept/hide/show, CSV export, 390 px viewport, console and exception log | macOS, Chrome headless, `tests/manual/report-ui.py` served over `http://127.0.0.1` | 42 checks passed, no console errors, no horizontal scroll |
| 2026-09-07 | PIM activation detection on a real tenant, without touching a role the account already held: a 3 h eligibility for Directory Readers, activated for 1 h, `Get-RiskyRoleAssignment -SkipAzure -AdditionalEntraRole 'Directory Readers'`, `Remove- -WhatIf` on both findings | Own tenant (Entra ID P2), interactive sign-in with the write scope, module from the working tree | 6 findings in 4 s. The activation was typed `Activated` and protected ("PIM activation, deactivate or remove the eligibility in PIM"), score 3.0 Low; the eligibility `Eligible` and protected, score 1.5 Info; no `Permanent` finding for the test role; the account's permanent Global Administrator still reported at 9.0 and protected as the auditing identity; `Remove- -WhatIf` skipped both. 7 checks passed. Cleanup needed a second run: PIM refused the deactivation right after activating |
